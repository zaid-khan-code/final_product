export const proxyMatcher = [
  '/',
  '/login',
  '/launchpad',
  '/dashboard/:path*',
  '/employees/:path*',
  '/attendance/:path*',
  '/leave/:path*',
  '/me/:path*',
  '/config',
  '/config/:path*',
]
